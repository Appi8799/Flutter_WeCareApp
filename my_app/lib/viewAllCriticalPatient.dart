import 'package:flutter/material.dart';

class ViewCriticalPatient extends StatelessWidget {
  const ViewCriticalPatient({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('All Critical Patients'),
      ),
    );
  }
}
